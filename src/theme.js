import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#80d8ff",
    },
    secondary: {
      main: "#1c16c9",
    },
    background: {
      default: "#333",
    },
  },
});
