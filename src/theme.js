import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#002e3e",
    },
    secondary: {
      main: "#e04c32",
    },
    background: {
      default: "#ebe3df",
    },
  },
});
